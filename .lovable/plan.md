

## Plan: Import 37 Users with Email as Password

### What we'll do
1. **Update the `import-users` edge function** to set the password as the user's email address (instead of a random UUID) and set `email_confirm: true` so they can login immediately.

2. **Call the edge function** with the 37 user entries extracted from the uploaded image to create all accounts.

### User list (from image)
37 users from ENQ-001 to ENQ-037, each with their email. Password for each user = their email address.

### Technical details

**Edge function change (`supabase/functions/import-users/index.ts`)**:
- Line 98: Change `const tempPassword = \`\${crypto.randomUUID()}Aa!1\`;` → `const tempPassword = entry.email;`
- Line 102: Change `email_confirm: false` → `email_confirm: true`

**Then invoke the function** with all 37 entries to bulk-create the users. The `handle_new_user` trigger will automatically create profiles with ENQ-XXXX IDs and assign the 'user' role.

### Important notes
- Existing users (already in the system) will be skipped or have their names updated
- Each user can login with: **Email** = their email, **Password** = their email
- The ENQ IDs will be auto-generated by the `generate_user_id()` function (so they may not match the exact ENQ numbers in the image if some users already exist)

